# getemployed-learn-readworldapp

# What all are included in this project
# MakeFile, requirements, source, test, dockerfile.
# inside make file, we use lint, format, pytest    


1. Create a python Virtual Environment 'virtualenv ~/.venv'
2. Create empty files: 'Makefile', 'requirements.txt', 'main.py', 'Dockerfile', 'mylib/__init__.py'
3. Populate 'MakeFile'
